services:
  # 1. Backend Server (Node.js)
  - type: web
    name: public-grievance-backend
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: MONGODB_URI
        value: mongodb+srv://nagarevivek206_db_user:7sEHEKssevVynHVB@cluster0.sqyv5qq.mongodb.net/pgrs?retryWrites=true&w=majority&appName=Cluster0
      - key: OPENAI_API_KEY
        value: "" # REQUIRED: Set this manually in Render Dashboard for AI routing to work
      - key: JWT_SECRET
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        value: 225202389234-1mu2smt3l6265cj9rskmnsr8aeoai97c.apps.googleusercontent.com
      - key: NODE_ENV
        value: production

  # 2. Frontend (Static Site)
  - type: static
    name: public-grievance-frontend
    runtime: static
    rootDir: client
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: VITE_API_URL
        value: https://public-grievance-backend.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
